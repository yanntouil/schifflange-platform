/**
 * Placeholder:
 * Use placeholders only if the placeholder content is required.
 */

export const Placeholder = ({ children }: { children: React.ReactNode }) => {
  return <span className='text-default/50'>{children}</span>
}
