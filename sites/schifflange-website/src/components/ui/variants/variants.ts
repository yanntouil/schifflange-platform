/**
 * index for variants
 * use as variants.button, variants.scrollbar, variants.disabled, variants.focus
 */

export { buttonVariants as button } from "./button"
export { linkVariants as link } from "./link"
export { scrollbarVariants as scrollbar } from "./scrollbar"
export { disabledVariants as disabled, focusVariants as focus } from "./state"
