import { Option } from "@mobily/ts-belt";
/**
 * oneIsNotNullable
 * Check if at least one param is not null or undefined
 */
export declare const oneIsNotNullable: (...params: Option<unknown>[]) => boolean;
