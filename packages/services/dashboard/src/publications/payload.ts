export type Update = {
  publishedById?: string | null // ref to a workspace member
  publishedAt?: string | null // iso date
  publishedFrom?: string | null // iso date
  publishedTo?: string | null // iso date
}
